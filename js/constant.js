"use strict";

(() => {
  const LEFT_MOUSE_BUTTON = [0, 4];
  const ENTER_KEY = `Enter`;
  const ESC_KEY = `Escape`;

  window.constant = {
    LEFT_MOUSE_BUTTON,
    ENTER_KEY,
    ESC_KEY
  };
})();
